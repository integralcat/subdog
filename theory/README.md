# Theory + Examples

This folder holds background notes and educational write-ups that support the
main `subdog` script.

- `theory/rate_limit_theory.md` - leaky bucket vs token bucket
- `theory/modern_web_routing.md` - DNS -> TCP -> TLS (SNI) -> HTTP routing
- `theory/faq.md` - DNS/subdomain enumeration FAQ
